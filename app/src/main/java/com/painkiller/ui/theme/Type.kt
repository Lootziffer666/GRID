package com.painkiller.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography is intentional in Gate 0. Custom font
// hierarchy is deferred until a later gate that needs it.
val PainkillerTypography = Typography()
